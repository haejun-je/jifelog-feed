spring:
  application:
    name: jifelog-feed
  config:
    import:
      - "optional:classpath:infra-postgres.yaml"

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health