apiVersion: v1
kind: Service
metadata:
  name: feed-api
  labels:
    app: feed-api
spec:
  type: ClusterIP
  selector:
    app: feed-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
